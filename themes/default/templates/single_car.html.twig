{% extends 'base.html.twig' %}

{% set active_page = pageName %}

{% block title %}
{{ active_page }}
{% endblock %}



{% block unique_page_content %}
    <div class="uk-grid">
        <div class="uk-width-medium-4-5 uk-container-center uk-panel">
            <div class="uk-panel uk-panel-box uk-panel-header heading-panel">
                <h1 class="uk-panel-title">Sales</h1>
                <p>We stock a good range of affordable, quality used cars. Have a look at what we currently have in stock. Our stock is fully serviced and NCT'd before every sale</p>
            </div>
        </div>
    </div>


    <div class="uk-grid-width-medium-1-2 uk-grid-width-1-1 uk-grid-small">
                <div class="uk-panel uk-panel-box-primary uk-container-center">
                    <div>

                        <a href="{{ cars.image|raw }}" data-uk-lightbox="{group:'group1'}" title="{{ cars.make }} {{ cars.model }}">
                            <img src="{{ cars.image|raw }}" width="200" height="300" alt="">
                        </a>

                    </div>
                    <ul class="uk-list uk-text-center">

                        {#<li><img src="{{ cars.image|raw }}"></li>#}
                        <li>{{ cars.make|upper }}</li>
                        <li>{{ cars.model|capitalize }}</li>
                        <li>{{ cars.reg }}</li>
                        <li>{{ cars.mileage }}</li>
                        <li>{{ cars.color }}</li>
                        <li>{{ cars.body }}</li>
                        <li>{{ cars.owners }}</li>
                        <li>{{ cars.price }}</li>
                    </ul>
                </div>
    </div>
{% endblock %}
